export const SolscanTokenUrl = (TokenAddress: string) => {
  return `https://solscan.io/token/${TokenAddress}`;
};

export const SolscanAddressUrl = (Address: string) => {
  return `https://solscan.io/address/${Address}`;
};
